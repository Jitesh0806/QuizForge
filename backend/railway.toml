[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python manage.py migrate && gunicorn quiz_api.wsgi --log-file -"
healthcheckPath = "/api/auth/me/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
